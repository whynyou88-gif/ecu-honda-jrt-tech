<UserControl x:Class="JRT.Tect.Desktop.Views.DynoSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="#FFFFFF">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <TextBlock Text="⚙️ PENGATURAN GPS ROAD DYNO &amp; PROFIL KENDARAAN" Foreground="#00E5FF" FontWeight="ExtraBold" FontSize="14" Margin="0,0,0,16"/>

            <UniformGrid Columns="2">
                <!-- VEHICLE PROFILE -->
                <Border Background="#15161D" BorderBrush="#222228" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="🏍️ PROFIL KENDARAAN" Foreground="#FF5722" FontWeight="Bold" FontSize="12" Margin="0,0,0,12"/>
                        
                        <TextBlock Text="Total Bobot Kendaraan + Pengendara (kg):" Foreground="#A0AAB0" FontSize="11"/>
                        <TextBox Text="{Binding Settings.Profile.Weight}" Height="28" Background="#06090F" Foreground="White" Margin="0,4,0,12" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Rasio Kalibrasi RPM (Speed km/h -> RPM):" Foreground="#A0AAB0" FontSize="11"/>
                        <TextBox Text="{Binding Settings.Profile.RpmRatio}" Height="28" Background="#06090F" Foreground="White" Margin="0,4,0,12" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Suhu Udara Lingkungan (°C):" Foreground="#A0AAB0" FontSize="11"/>
                        <TextBox Text="{Binding Settings.Profile.AirTemp}" Height="28" Background="#06090F" Foreground="White" Margin="0,4,0,12" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Tekanan Udara Barometrik (hPa):" Foreground="#A0AAB0" FontSize="11"/>
                        <TextBox Text="{Binding Settings.Profile.AirPress}" Height="28" Background="#06090F" Foreground="White" Margin="0,4,0,0" VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- HARDWARE & TEST PARAMETERS -->
                <Border Background="#15161D" BorderBrush="#222228" BorderThickness="1" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="📡 MODUL GPS &amp; PENGUJIAN" Foreground="#00E5FF" FontWeight="Bold" FontSize="12" Margin="0,0,0,12"/>

                        <TextBlock Text="Serial Port GPS (UBLOX M10N / FTDI):" Foreground="#A0AAB0" FontSize="11"/>
                        <TextBox Text="{Binding Settings.GpsPort}" Height="28" Background="#06090F" Foreground="White" Margin="0,4,0,12" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Durasi Perekaman Fase Losses (detik):" Foreground="#A0AAB0" FontSize="11"/>
                        <TextBox Text="{Binding Settings.LossTime}" Height="28" Background="#06090F" Foreground="White" Margin="0,4,0,12" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Correction Factor Power (Standard 1.0):" Foreground="#A0AAB0" FontSize="11"/>
                        <TextBox Text="{Binding Settings.CorrectionFactor}" Height="28" Background="#06090F" Foreground="White" Margin="0,4,0,12" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Kalman Filter Acceleration Power:" Foreground="#A0AAB0" FontSize="11"/>
                        <TextBox Text="{Binding Settings.FilterPower}" Height="28" Background="#06090F" Foreground="White" Margin="0,4,0,0" VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
