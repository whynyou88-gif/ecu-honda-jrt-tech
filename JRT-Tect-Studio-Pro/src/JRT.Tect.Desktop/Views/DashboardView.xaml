<UserControl x:Class="JRT.Tect.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="#FFFFFF">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <!-- TOP KPI CARDS GRID -->
            <UniformGrid Columns="4" Margin="0,0,0,20">
                <Border Background="#15161D" BorderBrush="#222228" BorderThickness="1" CornerRadius="10" Padding="18,16" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="⏱ SYSTEM UPTIME" Foreground="#8E8E93" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="{Binding UptimeText}" Foreground="White" FontSize="26" FontWeight="ExtraBold" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                <Border Background="#15161D" BorderBrush="#222228" BorderThickness="1" CornerRadius="10" Padding="18,16" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="⚡ BATTERY VOLTAGE" Foreground="#8E8E93" FontSize="11" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="{Binding BatteryVoltage, StringFormat={}{0:F2}}" Foreground="#47FF7A" FontSize="26" FontWeight="ExtraBold"/>
                            <TextBlock Text=" V" Foreground="#47FF7A" FontSize="16" FontWeight="Bold" VerticalAlignment="Bottom" Margin="2,0,0,3"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border Background="#15161D" BorderBrush="#222228" BorderThickness="1" CornerRadius="10" Padding="18,16" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="⚙ ECU CONNECTION" Foreground="#8E8E93" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="READY" Foreground="#00E5FF" FontSize="26" FontWeight="ExtraBold" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                <Border Background="#15161D" BorderBrush="#222228" BorderThickness="1" CornerRadius="10" Padding="18,16">
                    <StackPanel>
                        <TextBlock Text="🌡 CPU TEMP" Foreground="#8E8E93" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="38.0 °C" Foreground="#FF9800" FontSize="26" FontWeight="ExtraBold" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- MAIN GAUGE SWEEP PANEL -->
            <Border Background="#15161D" BorderBrush="#FF5722" BorderThickness="1" CornerRadius="12" Padding="24" Margin="0,0,0,20">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="ENGINE RPM SWEEP" Foreground="#8E8E93" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <Border Width="200" Height="200" CornerRadius="100" BorderBrush="#FF5722" BorderThickness="6" Background="#0A0A0E">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Rpm}" Foreground="#00E5FF" FontSize="44" FontWeight="ExtraBold" HorizontalAlignment="Center"/>
                            <TextBlock Text="RPM" Foreground="#FF5722" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <UniformGrid Columns="3" Width="500" Margin="0,20,0,0">
                        <Border Background="#0E0E12" CornerRadius="8" Padding="12,8" Margin="0,0,8,0">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="SPEED" Foreground="#8E8E93" FontSize="10" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Speed, StringFormat={}{0:F0} km/h}" Foreground="White" FontSize="18" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#0E0E12" CornerRadius="8" Padding="12,8" Margin="0,0,8,0">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="THROTTLE (TPS)" Foreground="#8E8E93" FontSize="10" FontWeight="Bold"/>
                                <TextBlock Text="{Binding TpsPercent, StringFormat={}{0:F1} %}" Foreground="#47FF7A" FontSize="18" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#0E0E12" CornerRadius="8" Padding="12,8">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="ENGINE TEMP" Foreground="#8E8E93" FontSize="10" FontWeight="Bold"/>
                                <TextBlock Text="{Binding EngineTemp, StringFormat={}{0:F1} °C}" Foreground="#FF9800" FontSize="18" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
