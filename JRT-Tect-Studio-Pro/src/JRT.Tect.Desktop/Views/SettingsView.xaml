<UserControl x:Class="JRT.Tect.Desktop.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="#FFFFFF">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <UniformGrid Columns="2" Margin="0,0,0,16">
                <!-- K-LINE / ECU SETTINGS -->
                <Border Background="#15161D" BorderBrush="#222228" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="⚙ K-LINE / ECU COMMUNICATION" Foreground="#00E5FF" FontWeight="Bold" FontSize="12" Margin="0,0,0,12"/>
                        <TextBlock Text="UART Baudrate:" Foreground="#A0AAB0" FontSize="11"/>
                        <TextBox Text="{Binding BaudRate}" Height="28" Background="#06090F" Foreground="White" Margin="0,4,0,12" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Timeout (ms):" Foreground="#A0AAB0" FontSize="11"/>
                        <TextBox Text="{Binding TimeoutMs}" Height="28" Background="#06090F" Foreground="White" Margin="0,4,0,12" VerticalContentAlignment="Center"/>

                        <CheckBox Content="Auto Reconnect pada K-Line drop" IsChecked="{Binding AutoReconnect}" Foreground="#47FF7A"/>
                    </StackPanel>
                </Border>

                <!-- INTERFACE SETTINGS -->
                <Border Background="#15161D" BorderBrush="#222228" BorderThickness="1" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="🎨 INTERFACE &amp; THEME" Foreground="#00E5FF" FontWeight="Bold" FontSize="12" Margin="0,0,0,12"/>
                        <CheckBox Content="Dark Mode (Carbon Cockpit Theme)" IsChecked="{Binding DarkMode}" Foreground="White" Margin="0,0,0,12"/>
                        <Button Content="💾 Save Settings" Background="#FF5722" Foreground="White" FontWeight="Bold" Padding="16,8" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
