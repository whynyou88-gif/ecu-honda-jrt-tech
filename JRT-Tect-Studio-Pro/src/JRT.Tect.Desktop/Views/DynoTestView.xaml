<UserControl x:Class="JRT.Tect.Desktop.Views.DynoTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="#FFFFFF">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <!-- DYNO RUN COMPARISON SUMMARY -->
            <Border Background="#121A24" BorderBrush="#00E5FF" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Run #1 (Stock Base)" Foreground="#FF5722" FontWeight="Bold" FontSize="13"/>
                        <TextBlock Text="Peak HP: 8.5 HP @ 6300 RPM" Foreground="#A0AAB0" FontSize="11" Margin="0,2,0,0"/>
                        <TextBlock Text="Peak Tq: 10.8 Nm @ 5000 RPM" Foreground="#A0AAB0" FontSize="11"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Run #2 (Remap Stage 1)" Foreground="#00E5FF" FontWeight="Bold" FontSize="13"/>
                        <TextBlock Text="Peak HP: 10.4 HP @ 6700 RPM" Foreground="#47FF7A" FontSize="11" Margin="0,2,0,0"/>
                        <TextBlock Text="Peak Tq: 12.2 Nm @ 5500 RPM" Foreground="#47FF7A" FontSize="11"/>
                    </StackPanel>
                    <Border Grid.Column="2" Background="#06090F" CornerRadius="6" Padding="16,8" BorderBrush="#47FF7A" BorderThickness="1">
                        <TextBlock Text="{Binding HpGainText}" Foreground="#47FF7A" FontWeight="ExtraBold" FontSize="14"/>
                    </Border>
                </Grid>
            </Border>

            <!-- DYNO GRAPH CANVAS PLACEHOLDER -->
            <Border Background="#06090F" BorderBrush="#222228" BorderThickness="1" CornerRadius="8" Height="400" Padding="20">
                <Grid>
                    <TextBlock Text="⚡ DYNO GRAPH VISUALIZER (HORSEPOWER &amp; TORQUE VS RPM)" Foreground="#66666E" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
