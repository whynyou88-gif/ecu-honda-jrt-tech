<UserControl x:Class="JRT.Tect.Desktop.Views.MapEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="#FFFFFF">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP MODEL & ACTION TOOLBAR -->
        <Border Grid.Row="0" Background="#15161D" BorderBrush="#222228" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <DockPanel>
                <ComboBox SelectedItem="{Binding SelectedEcuModel}" Width="360" Height="30" Background="#06090F" Foreground="White" FontWeight="Bold" DockPanel.Dock="Left" Margin="0,0,12,0" VerticalContentAlignment="Center">
                    <ComboBoxItem Content="22 VARIO 125 OLD KZRA-601 (2013-2015)"/>
                    <ComboBoxItem Content="38770-K59-A11 (VARIO 125 eSP)"/>
                    <ComboBoxItem Content="38770-K25-901 (BEAT FI)"/>
                </ComboBox>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Content="Set" Background="#121A24" Foreground="White" Width="50" Margin="0,0,6,0"/>
                    <Button Content="+ Add" Command="{Binding IncreaseCellValuesCommand}" Background="#00E5FF" Foreground="Black" FontWeight="Bold" Width="60" Margin="0,0,6,0"/>
                    <Button Content="- Sub" Command="{Binding DecreaseCellValuesCommand}" Background="#FF5722" Foreground="White" FontWeight="Bold" Width="60" Margin="0,0,12,0"/>
                    <TextBlock Text="Step:" Foreground="#A0AAB0" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox Text="{Binding EditStep}" Width="50" Height="28" Background="#06090F" Foreground="White" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- QUICK PRESET BUTTONS -->
        <Border Grid.Row="1" Background="#121A24" BorderBrush="#FF5722" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🚁 MAP PARAMETER PRESETS: " Foreground="#FF5722" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <Button Content="Helicopter Idle" Background="#1E293B" Foreground="White" Padding="8,4" Margin="0,0,6,0"/>
                <Button Content="Rotary Idle" Background="#1E293B" Foreground="White" Padding="8,4" Margin="0,0,6,0"/>
                <Button Content="Pop &amp; Bangs" Background="#1E293B" Foreground="White" Padding="8,4" Margin="0,0,6,0"/>
                <Button Content="Launch Control" Background="#1E293B" Foreground="White" Padding="8,4" Margin="0,0,6,0"/>
                <Button Content="Cut Lock (10.5k)" Background="#D32F2F" Foreground="White" FontWeight="Bold" Padding="8,4"/>
            </StackPanel>
        </Border>

        <!-- 2D / 3D MAP DATA GRID -->
        <Border Grid.Row="2" Background="#06090F" BorderBrush="#222228" BorderThickness="1" CornerRadius="8" Padding="12">
            <DataGrid ItemsSource="{Binding MapGrid}" AutoGenerateColumns="False" IsReadOnly="False" Background="#06090F" Foreground="White" GridLinesVisibility="All" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Row (RPM)" Binding="{Binding RowHeader}" Width="100" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Col (TPS %)" Binding="{Binding ColumnHeader}" Width="100" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Fuel / Ignition Value" Binding="{Binding Value, StringFormat={}{0:F3}}" Width="*"/>
                    <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="80" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
