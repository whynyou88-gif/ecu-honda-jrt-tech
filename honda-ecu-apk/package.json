{
  "name": "honda-ecu-tool",
  "version": "1.0.0",
  "description": "Honda ECU K-Line Diagnostic & Remapping Tool — Android APK",
  "main": "index.js",
  "scripts": {
    "build": "echo Web assets ready in www/",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap sync android && cd android && .\\gradlew.bat assembleDebug",
    "cap:build:release": "npx cap sync android && cd android && .\\gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "6.1.2",
    "@capacitor/core": "6.1.2",
    "@capacitor/network": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "6.1.2"
  }
}
